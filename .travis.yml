language: python

python:
  - "3.5"

install:
  - pip install --upgrade pip
  - pip install netcdf4 scipy numba
  - pip list

script:
  - make raw.md5
  - make seawifs_ocn_mask.nc
  - travis_wait 60 make TOPO=seawifs_ocn_mask.nc
  - md5sum *.nc
